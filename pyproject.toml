[project]
name = "ocr-compare"
version = "0.1.0"
description = "GUI tool comparing text-extraction / OCR / VLM engines for RAG ETL evaluation"
requires-python = ">=3.11"

[project.scripts]
ocr-compare = "app.main:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
