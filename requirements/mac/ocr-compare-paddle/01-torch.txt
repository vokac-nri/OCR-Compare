# Seeded from Windows pins 2026-06, never installed on a real Mac — on pip
# failure see README "macOS first run" (setup_env.sh --unpinned +
# freeze_pins.py --write).
# Stage 1: torch exists only to satisfy paddleocr -> modelscope -> torch.
# Keeping the paddle family in its own env also sidesteps the classic
# torch+paddle duplicate-libomp abort (OMP Error #15). Install FIRST.
torch==2.12.0
