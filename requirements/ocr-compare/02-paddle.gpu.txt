# Stage 2 (gpu): CUDA paddle from the PaddlePaddle index. If
# paddle.utils.run_check() fails after this, setup_env.ps1 falls back to the
# .cpu manifest and records paddle_effective=cpu so the checker stays in sync.
--index-url https://www.paddlepaddle.org.cn/packages/stable/cu126/
paddlepaddle-gpu==3.3.1
