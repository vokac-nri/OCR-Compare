# Stage 1 (gpu): CUDA torch from the PyTorch index. Must install BEFORE
# paddle and the PyPI bulk so resolver backtracking never swaps in CPU wheels.
--index-url https://download.pytorch.org/whl/cu126
torch==2.12.0+cu126
torchvision==0.27.0+cu126
